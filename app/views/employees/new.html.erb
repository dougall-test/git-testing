<%= form_for @employee do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>
</br>
	<%= f.label :salary_in_dollars %>
	<%= f.text_field :salary_in_dollars, :value => (number_with_precision(f.object.salary_in_dollars, :precision => 2) || 0) %>
</br>
	<%= f.submit "Submit" %>
<% end %>


