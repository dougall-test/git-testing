<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value_range_start %><br />
    <%= f.number_field :value_range_start %>
  </div>
  <div class="field">
    <%= f.label :value_range_end %><br />
    <%= f.number_field :value_range_end %>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
